---
sudo: false
language: ruby
cache: bundler
rvm:
  - 2.2.0
  - 2.3.0
  - 2.4.0
  - 2.5.0
  - 2.6.0
before_install:
  - gem uninstall -v '>= 2' -i $(rvm gemdir)@global -ax bundler || true
  - gem install bundler -v '< 2'
env:
  - DISABLE_SIMPLECOV=true
